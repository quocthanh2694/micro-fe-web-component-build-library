import { URI } from "src/constants/constant";

export const getPageURI = (target: URI) => {
    // const rootPath = useCurrentResolvedPath();
    // console.log('@@root path', rootPath)

    return `${target}`
    // return `${BASE_URL}${target}`
}